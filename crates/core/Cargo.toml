[package]
name = "shadowshield-core"
version = "0.1.0-dev"
edition = "2021"

[dependencies]
shadowshield-protocol = { path = "../protocol" }
shadowshield-detectors = { path = "../detectors" }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
